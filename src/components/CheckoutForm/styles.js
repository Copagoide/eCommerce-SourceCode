import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  button: {
    background: '#34a0a4',
    '&:hover': {
      background: '#006d77',
    },
  },
}));